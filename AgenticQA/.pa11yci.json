{
  "defaults": {
    "chromeLaunchConfig": {
      "args": [
        "--no-sandbox",
        "--disable-setuid-sandbox",
        "--disable-dev-shm-usage"
      ]
    },
    "timeout": 30000,
    "wait": 1000
  },
  "urls": [
    "http://localhost:8080"
  ]
}
