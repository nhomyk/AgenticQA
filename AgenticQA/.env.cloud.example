# ============================================================================
# AgenticQA - Weaviate Cloud Configuration
# ============================================================================
# This is an example configuration for deploying AgenticQA with Weaviate Cloud.
# Copy this file and fill in your actual values:
#
#   cp .env.cloud.example .env.cloud
#   source .env.cloud
#
# Then run AgenticQA which will use this configuration.
# ============================================================================

# Deployment Mode
# Options: local, cloud, custom
# Use 'cloud' for Weaviate Cloud, 'local' for Docker, 'custom' for self-hosted
export AGENTICQA_RAG_MODE=cloud

# Weaviate Cloud Configuration
# Get these from your Weaviate Cloud console at https://console.weaviate.cloud

# Cluster hostname (from your Weaviate Cloud console)
# Example: my-project-abc123.weaviate.network
export WEAVIATE_HOST=your-cluster-name.weaviate.network

# API key for authentication (keep this secret!)
# Create a new API key in your Weaviate Cloud console under "API Keys"
export WEAVIATE_API_KEY=your-api-key-here

# Collection name (optional, defaults to "AgenticQADocuments")
# You can use different collections for different environments (dev, staging, prod)
# export WEAVIATE_COLLECTION=AgenticQADocuments

# ============================================================================
# Optional: Additional Configuration
# ============================================================================

# OpenAI API key (if using SemanticEmbedder for ML-based embeddings)
# export OPENAI_API_KEY=sk-...

# ============================================================================
# Usage in Pipeline
# ============================================================================
#
# 1. Local Development (Docker):
#    export AGENTICQA_RAG_MODE=local
#    # No API key needed for local mode
#    python -m agenticqa.cli --command test_rag
#
# 2. Cloud Production (Weaviate Cloud):
#    export AGENTICQA_RAG_MODE=cloud
#    export WEAVIATE_HOST=cluster-name.weaviate.network
#    export WEAVIATE_API_KEY=your-api-key
#    python -m agenticqa.cli --command test_rag
#
# 3. Custom Endpoint (Self-Hosted):
#    export AGENTICQA_RAG_MODE=custom
#    export WEAVIATE_HOST=my-server.example.com
#    export WEAVIATE_API_KEY=optional-api-key
#    python -m agenticqa.cli --command test_rag
#
# ============================================================================
# Troubleshooting
# ============================================================================
#
# Connection Issues?
# 1. Verify WEAVIATE_HOST is correct (check Weaviate Cloud console)
# 2. Verify WEAVIATE_API_KEY is set and valid
# 3. Check network connectivity: curl https://<WEAVIATE_HOST>/v1/
#
# Still having issues?
# See WEAVIATE_SETUP.md for complete troubleshooting guide
#
# ============================================================================
