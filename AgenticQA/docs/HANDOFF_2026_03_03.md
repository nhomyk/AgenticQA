# Session Handoff — 2026-03-03

## What Was Done This Session

### 1. OpenAPI / Swagger Docs — COMPLETE
- **165 endpoints tagged** across 36 named groups in `agent_api.py`
- Tag groups: Health, System, Agents, Agent Factory, Workflows, Chat, Pipeline, Observability,
  Red Team, Security, Compliance, Safety, SDET, Datastore, Learning, Developer Intelligence,
  Provenance, GitHub, Release, Scoring, Remediation, Scan Trends, Regression, Code Analysis,
  Workspace, Notifications, Operator, Onboarding, Plugin, Ingest, Testing, Demo, Badge,
  Benchmark, Custom Rules
- `FastAPI()` constructor enriched with: description (auth notes + GitHub link), contact,
  MIT license, and full `openapi_tags` list with descriptions for each group
- `/docs` (Swagger UI) and `/redoc` now fully organized and searchable
- 153/154 HTTP endpoint tests pass (1 pre-existing failure: `test_red_team_scan` needs live services)

### 2. Resumes Created This Session
- `docs/RESUME_STAFF_SDET_PLATFORM_NICK_HOMYK.txt` — Staff SDET at mission-driven org
  (React/Next.js, FinTech/regulated, 0-to-1 builder, Blue Bite acquisition centerpiece)
- `docs/RESUME_STEALTH_AI_INFRA_SDET_NICK_HOMYK.txt` — Stealth AI infra startup SDET
  (distributed systems, Python, Docker, framework-from-scratch, stress testing focus)

### 3. Industry Standard Features (previous session, now committed)
- Dependabot on all 4 repos (AgenticQA + 3 action repos)
- CodeQL on all 3 action repos
- pip-audit CI job (`continue-on-error: true` for baseline)
- Pre-commit hooks (black, flake8, mypy, bandit, standard hooks)
- CODEOWNERS (all files + security-sensitive paths)
- OpenSSF Scorecard workflow + README badge
- Coverage threshold gate (`--cov-fail-under=60`)
- Weaviate pinned from `:latest` to `1.27.0`

---

## Current State

### Tests
- 2308 unit tests passing (pre-existing: `test_sre_output_drives_sdet_coverage_check` fails
  due to corrupt local JSON data file in `.agenticqa/` — unrelated to any code change)
- 153/154 HTTP endpoint tests pass
- All 8 agents execute cleanly

### Known Pre-Existing Failures (do not fix unless specifically targeting)
- `test_sre_output_drives_sdet_coverage_check` — `JSONDecodeError` in pattern_analyzer data
  file at `.agenticqa/`. Delete corrupted file to fix locally.
- `test_red_team_scan` HTTP test — returns 500 without live Weaviate/Neo4j

---

## P2 Development Queue (priority order)

### 1. Semantic Releases for Action Repos (next up, low effort)
Use `release-please` or `semantic-release` so action versions bump automatically on merge.
- Target repos: `nhomyk/mcp-scan-action`, `nhomyk/eu-ai-act-check-action`, `nhomyk/agenticqa-scan-action`
- Add `.release-please-config.json` + `release-please-manifest.json` to each
- Or use the `google-github-actions/release-please-action` in a workflow

### 2. Coverage Threshold Bump
- Current gate: `--cov-fail-under=60`
- After first clean CI run, tighten to 70% then 75%
- File: `.github/workflows/tests.yml`, the `pytest` run in the `unit-tests` job

### 3. Authenticated GitHub Action Path
- Current 3 Marketplace actions work with no API key (always)
- Add optional `api_url` + `api_token` inputs to each action
- When provided, call the live AgenticQA API instead of local scan
- This closes the loop: free Marketplace tool -> paying customer

### 4. Agent Observability Live Dashboard
- Data already exists in `LearningMetricsSnapshot` (`~/.agenticqa/metrics_history.jsonl`)
- Add a real-time Grafana/Plotly page showing agent run times, failure rates, learning curves
- `GET /api/learning-metrics` already returns this data

---

## Pending Content to Post

| File | Platform | Status |
|------|----------|--------|
| `docs/LINKEDIN_MCP_SECURITY_SCANNER_NICK_HOMYK.md` | LinkedIn | Not posted |
| `docs/MEDIUM_MCP_SECURITY_SCANNER_NICK_HOMYK.md` | Medium | Not posted |
| `docs/LINKEDIN_EU_AI_ACT_CICD_NICK_HOMYK.md` | LinkedIn | Not posted |
| `docs/MEDIUM_EU_AI_ACT_CICD_NICK_HOMYK.md` | Medium | Not posted |

---

## Pending Job Application Items

| Item | Status |
|------|--------|
| Aker cover letter (`COVER_LETTER_AKER_AI_PLATFORM_ENGINEERING_NICK_HOMYK.txt`) | Contains em dashes — needs fix |
| Staff SDET cover letter | Not yet created |
| Stealth AI infra SDET cover letter | Not yet created |

**User preference**: No em dashes anywhere. Use commas, colons, or new sentences instead of ` - ` used as a dash.

---

## Key File Locations

| What | Where |
|------|-------|
| Main API file (165 tagged endpoints) | `agent_api.py` |
| All 8 agents | `src/agents.py` |
| OpenAPI docs live at | `/docs` and `/redoc` when server is running |
| Aker resume | `docs/RESUME_AKER_AI_PLATFORM_ENGINEERING_NICK_HOMYK.txt` |
| Staff SDET resume | `docs/RESUME_STAFF_SDET_PLATFORM_NICK_HOMYK.txt` |
| Stealth AI infra resume | `docs/RESUME_STEALTH_AI_INFRA_SDET_NICK_HOMYK.txt` |
| Memory | `.claude/projects/.../memory/MEMORY.md` |
| Roadmap | `.claude/projects/.../memory/roadmap.md` |

---

## Git / Repo Notes

- Git root: `/Users/nicholashomyk/mono`
- GitHub: `https://github.com/nhomyk/AgenticQA.git`
- Main branch: `main`
- Always update BOTH READMEs when changing docs: `/mono/README.md` and `/mono/AgenticQA/README.md`
- macOS username: `nicholashomyk`, GitHub username: `nhomyk`
