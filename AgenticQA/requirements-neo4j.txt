# Neo4j Graph Database Requirements
# Install with: pip install -r requirements-neo4j.txt

# Core Neo4j driver
neo4j>=5.15.0

# For beautiful analytics output
rich>=13.0.0

# For GraphRAG integration (combine with existing requirements.txt)
# These should already be installed if you have base AgenticQA setup:
# - weaviate-client>=3.0.0
# - sentence-transformers>=2.0.0
# - ragas>=0.4.0
