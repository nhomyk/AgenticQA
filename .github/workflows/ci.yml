on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:
    inputs:
      reason:
        description: 'Reason for manual trigger'
        required: false
        default: 'Manual workflow trigger'

jobs:
  lint:
    runs-on: ubuntu-latest
    name: Code Linting
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run ESLint
        run: npx eslint . --ext .js

  unit-test:
    needs: [lint]
    runs-on: ubuntu-latest
    name: Unit Tests
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run Jest unit tests
        run: npx jest --coverage 2>&1 | tee jest-output.log
      - name: Save Jest failures
        if: failure()
        run: |
          mkdir -p test-failures
          if [ -f jest-output.log ]; then
            echo "JEST_FAILED=true" >> test-failures/summary.txt
            grep -i "error\|fail\|expected" jest-output.log >> test-failures/jest-errors.txt || true
          fi
      - name: Upload test failures
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-failures
          path: test-failures/
          retention-days: 1

  test-playwright:
    needs: [lint]
    runs-on: ubuntu-latest
    name: Playwright Tests
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps
      - name: Run Playwright tests
        run: npx playwright test 2>&1 | tee playwright-output.log
      - name: Save Playwright failures
        if: failure()
        run: |
          mkdir -p test-failures
          if [ -f playwright-output.log ]; then
            echo "PLAYWRIGHT_FAILED=true" >> test-failures/summary.txt
            grep -i "error\|fail\|expected" playwright-output.log >> test-failures/playwright-errors.txt || true
          fi
      - name: Upload test failures
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-failures
          path: test-failures/
          retention-days: 1

  test-vitest:
    needs: [lint]
    runs-on: ubuntu-latest
    name: Vitest Tests
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run Vitest
        run: npm run test:vitest -- --run --coverage 2>&1 | tee vitest-output.log
      - name: Save Vitest failures
        if: failure()
        run: |
          mkdir -p test-failures
          if [ -f vitest-output.log ]; then
            echo "VITEST_FAILED=true" >> test-failures/summary.txt
            grep -i "error\|fail\|expected" vitest-output.log >> test-failures/vitest-errors.txt || true
          fi
      - name: Upload test failures
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-failures
          path: test-failures/
          retention-days: 1

  test-cypress:
    needs: [lint]
    runs-on: ubuntu-latest
    name: Cypress Tests
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Install Cypress Browsers
        run: npx cypress install
      - name: Start server
        run: npm start > /tmp/server.log 2>&1 &
        timeout-minutes: 1
      - name: Wait for server to be ready
        run: |
          for i in {1..30}; do
            if curl -s http://localhost:3000 > /dev/null; then
              echo "✅ Server is ready!"
              exit 0
            fi
            echo "Waiting for server... ($i/30)"
            sleep 1
          done
          echo "❌ Server did not start in time"
          cat /tmp/server.log
          exit 1
      - name: Run Cypress tests
        run: npx cypress run 2>&1 | tee cypress-output.log
        timeout-minutes: 2
      - name: Save Cypress failures
        if: failure()
        run: |
          mkdir -p test-failures
          if [ -f cypress-output.log ]; then
            echo "CYPRESS_FAILED=true" >> test-failures/summary.txt
            grep -i "error\|fail\|expected" cypress-output.log >> test-failures/cypress-errors.txt || true
          fi
      - name: Upload test failures
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: test-failures
          path: test-failures/
          retention-days: 1

  sdet-agent:
    needs: [unit-test, test-playwright, test-vitest, test-cypress]
    if: always()
    runs-on: ubuntu-latest
    name: SDET Agent (Manual UI Testing)
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Install Playwright Browsers
        run: npx playwright install --with-deps chromium
      - name: Start server
        run: npm start &
      - name: Wait for server
        run: sleep 2
      - name: Run SDET Manual UI Testing
        run: |
          node << 'SDET_SCRIPT'
          const fs = require('fs');
          
          console.log('\n🤖 SDET Agent: Manual UI Testing & Codebase Analysis\n');
          console.log('═══════════════════════════════════════════════════════\n');
          
          // Codebase Knowledge
          console.log('📋 Codebase Knowledge Assessment:\n');
          console.log('  ✓ UI Framework: Node.js + Express');
          console.log('  ✓ Frontend: Vanilla JavaScript (app.js)');
          console.log('  ✓ Test Frameworks: Jest, Playwright, Cypress, Vitest');
          console.log('  ✓ Architecture: MVC with Puppeteer backend\n');
          
          // Verify files
          console.log('📂 Critical Codebase Files:\n');
          const requiredFiles = ['public/app.js', 'public/index.html', 'server.js', 'qa-agent.js'];
          requiredFiles.forEach(file => {
            const exists = fs.existsSync(file);
            console.log(`  ${exists ? '✅' : '❌'} ${file}`);
          });
          
          // UI Features
          console.log('\n🔍 UI Functionality Verification:\n');
          const appCode = fs.readFileSync('public/app.js', 'utf8');
          const features = [
            ['Download Script Button', appCode.includes('downloadScript')],
            ['Copy to Clipboard', appCode.includes('copyToClipboard')],
            ['Tab Switching', appCode.includes('tab-button')],
            ['Scan Functionality', appCode.includes('scanBtn.addEventListener')],
            ['Empty State Handling', appCode.includes('No issues detected')]
          ];
          features.forEach(([name, exists]) => {
            console.log(`  ${exists ? '✅' : '❌'} ${name}`);
          });
          
          console.log('\n✅ SDET Agent Assessment Complete\n');
          SDET_SCRIPT

  fullstack-agent:
    needs: [sdet-agent]
    if: always()
    runs-on: ubuntu-latest
    name: Fullstack Agent (Code Fixes)
    permissions:
      contents: write
      actions: read
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - name: Download test failure artifacts
        uses: actions/download-artifact@v4
        with:
          name: test-failures
          path: ./test-failures
        continue-on-error: true
      - name: Display downloaded artifacts
        run: |
          if [ -d test-failures ]; then
            echo "📦 Test failure artifacts downloaded:"
            ls -la test-failures/
            echo ""
            echo "Content preview:"
            find test-failures -type f -exec echo "--- {} ---" \; -exec head -5 {} \;
          else
            echo "No test failure artifacts found (tests may have passed)"
          fi
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Set up git credentials
        run: |
          git config --global user.name "fullstack-agent[bot]"
          git config --global user.email "fullstack-agent[bot]@users.noreply.github.com"
          git config --global url."https://x-access-token:${{ secrets.GITHUB_TOKEN }}@github.com/".insteadOf "https://github.com/"
      - name: Run Fullstack Agent
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: node fullstack-agent.js

  sre-agent:
    needs: [fullstack-agent]
    if: always()
    runs-on: ubuntu-latest
    name: SRE Agent (Code & Pipeline Fixes)
    permissions:
      contents: write
      actions: write
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Set up git credentials
        run: |
          git config --global user.name "github-actions[bot]"
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
      - name: Run Agentic SRE Engineer
        env:
          GH_PAT: ${{ secrets.GH_PAT }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: node agentic_sre_engineer.js

  compliance-agent:
    needs: [unit-test, test-playwright, test-vitest, test-cypress, sdet-agent]
    if: always()
    runs-on: ubuntu-latest
    name: Compliance Agent (Legal & Regulatory)
    steps:
      - uses: actions/checkout@v4
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Install dependencies
        run: npm ci
      - name: Run Compliance Audit
        run: npm run compliance-agent
      - name: Upload compliance report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: compliance-audit-report
          path: compliance-audit-report.md
          retention-days: 90
      - name: Check for compliance critical issues
        run: |
          if grep -q "🔴 Critical Issues.*must be resolved" compliance-audit-report.md; then
            echo "⚠️  Compliance audit found critical issues"
            exit 0
          fi
          echo "✅ Compliance audit passed"